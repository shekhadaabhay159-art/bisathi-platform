# Frontend Environment Variables
# For local dev: leave blank (Vite proxy handles /api → localhost:8000)
# For production: set this to your deployed backend URL (no trailing slash)
# Example: https://bisathi-api.onrender.com
VITE_API_URL=
